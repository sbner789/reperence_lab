# 2026-05-27 Decision

## PdfViewer 컴포넌트 구현

**요구사항:** `src/components/PdfViewer.tsx` 생성, 기능: 파일 업로드 / 페이지 이동 / 줌 인아웃 / 페이지 번호 표시

**수행 내용:**
- 라이브러리: `react-pdf@10.4.1` + `pdfjs-dist@5.4.296` (버전 일치)
- Worker: `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url)` 로컬 방식
- 파일 업로드: `<label>` 감싸기로 useRef 없이 구현
- Object State Pattern 적용 (`viewer`, `INITIAL_VIEWER`)

---

## PdfViewer 커스텀 훅 분리

**요구사항:** 로직과 UI 소스를 커스텀 훅으로 분리

**수행 내용:**
- `src/hooks/usePdfViewer.ts` 생성 — 상태 및 핸들러 로직 전담
- `src/components/PdfViewer.tsx` — UI만 담당, hook에서 값 주입

---

## 코드 스타일 가이드 업데이트

**요구사항:** state 변수명을 기능에 부합하는 의미 있는 이름으로 지정

**수행 내용:**
- `@code-style.md` 6번 항목 추가: `state` 같은 범용 이름 지양, 기능 기반 명명 규칙 명시

---

## ESLint any 타입 예외 처리

**요구사항:** `@api-guide.md`의 `Record<string, any>`, `T = any` 패턴이 ESLint `no-explicit-any: error` 규칙과 충돌 → `src/api/` 디렉토리에 한해 예외 처리

**수행 내용:**
- `eslint.config.js`에 `src/api/**` 파일 전용 override 추가
- 해당 경로에서만 `@typescript-eslint/no-explicit-any: 'off'` 적용
- 나머지 코드베이스는 기존 `error` 규칙 유지
